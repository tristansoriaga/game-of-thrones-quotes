import { Stack, StackProps } from '@chakra-ui/react';

export const Main = (props: StackProps) => <Stack justifyContent='center' alignItems='center' spacing='1.5rem' width='100%' minH='100vh' {...props} />;
